/**
 * @deprecated Import from '@/app/stores/session.store' directly.
 * This file exists only for backwards compatibility.
 */
export {
  useSessionStore,
  useIsAuthenticated,
  useIsLocked,
  useDerivedKey,
  useCurrentUser,
  useSession,
} from '@/app/stores/session.store';
